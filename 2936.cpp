#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int cur, boi, bot, map, iar;
    cin >> cur >> boi >> bot >> map >> iar;
    cout <<cur*300+boi*1500+bot*600+map*1000+iar*150+225 << "\n";
return 0;
}